==== BORRA EL COMPOSE ANGITUO (3 CONTENEDORES) ====

docker compose up -d

docker-compose exec app php artisan migrate

docker-compose exec app php artisan db:seed

# Clear route cache
docker-compose exec app php artisan route:clear
docker-compose exec app php artisan config:clear

'''
docker exec -it comb-db mysql -u el_usuario_pro -p

UnaConstaseñaCualquieraQueQuieres?UnRascacielos?YoQuieroUn240SX

use entrenamientos;

select * from ciclistas limit 100;
'''

==== 

docker compose exec app -it dawapp artisan make:migration addedauthor

# Create a new roller
docker-compose exec app php artisan make:controller AuthController

====

# Dicta que archivos añadir (. = TODO ; si quieres añadir solo 1, pon que 1 es)
# Para subir SOLO este archivo he hecho: git add 'commands to remember' ; git commit -m "fuera el git ignore" ; git push 
git add . ; 

# Añade al repositorio local (Stash)
git commit -m "fuera el git ignore" ;

# Subir
git push

# --- Modo pro, todo en 1 linea"
git add . ; git commit -m "ELCOMENTARIO" ; git push

# Si no deja hacer git pull porque tienes cambios pero no los quieres subir
git stash

# Ya luego git pull










# ----------

docker-compose exec app composer require laravel/ui:^2.0

# docker-compose exec app composer require laravel/ui:^2.0 --ignore-platform-reqs

docker-compose exec app php artisan ui bootstrap --auth

# This adds login and register... automatically?

@guest
    this is whats shown to guest user
@else
    this is whats shown to logged in user

# Not required?
docker-compose exec app npm install && npm run dev

# force to login
Goto home or whatever controller

add this:

    public function __construct(){
        $this->middleware('auth');
    }

